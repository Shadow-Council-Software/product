# Team / enterprise overrides for _bmad/config.toml.
# Committed on product/enterprise — switch branch when changing active product.
#
# [agents.bmad-agent-pm]
# description = "Prefers short, bulleted PRDs over narrative drafts."

[modules.bmm]
# Product hub; specialized Cursor workspaces: enterprise/ux, enterprise/platform (see enterprise/WORKSPACES.md)
planning_artifacts = "{project-root}/enterprise"
