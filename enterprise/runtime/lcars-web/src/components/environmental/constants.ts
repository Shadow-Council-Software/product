/**
 * W-CT-04 layout contract (openspec lcars-environmental):
 * at the certified viewport, Green authority QuickEnvPin spans full
 * Environmental panel width (no horizontal scroll); shared authority
 * is capped. Values cross-checked by engine/test/w-ct-04.test.ts.
 */
export const QUICK_ENV_PIN_VIEWPORT_WIDTH = 1280;
export const QUICK_ENV_PIN_SHARED_MAX_WIDTH = 480;
